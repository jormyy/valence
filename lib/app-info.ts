import { LEAGUES } from "./registry";

export const APP_NAME = "Valence";
export const APP_TITLE = "Valence — Live Sports Streams";
export const APP_DESCRIPTION = `Live sports streams — ${LEAGUES.map((league) => league.short).join(", ")}`;
